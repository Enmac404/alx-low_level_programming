#include "main.h"
/**
 * _strlen - return the length of a string
 * Return: 0 is success
 * @s: char to check
 * Description: this will return the length of a string
 */

int _strlen(char *)
{
	int a = 0;

	for (; *s++;)
		a++;
	return (a);
}
